name: Tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate JSON
        run: |
          python3 -m json.tool .agents/plugins/marketplace.json >/dev/null
          python3 -m json.tool plugins/cuebird-cx/.codex-plugin/plugin.json >/dev/null
          python3 -m json.tool plugins/cuebird-cx/hooks/hooks.json >/dev/null
      - name: Check shell syntax
        run: bash -n plugins/cuebird-cx/hooks/session-start.sh plugins/cuebird-cx/scripts/cuebird-cx.sh plugins/cuebird-cx/scripts/adapters/apple-reminders.sh tests/*.sh
      - name: Run machine-independent suite
        run: bash tests/run.sh
